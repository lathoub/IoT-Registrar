extends layout.pug

block content

  h1 #{title}
  p #{description}
  h3 IoT Registrar
  
  h2 Links to the main resources

  each link in links
    p #{link.rel} = 
      a(href= link.href)  #{link.title} 
      | (#{link.type})
